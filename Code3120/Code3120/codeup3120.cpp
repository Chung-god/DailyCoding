#include <stdio.h>
#include <math.h>
int main() {
	int temp, final;
	scanf_s("%d %d", &temp, &final);
	int i = 0;

	while (temp != final) {
		if (final > temp) {
			while(abs(temp - final)<)
		}
	}
	printf("%d\n", i);
}